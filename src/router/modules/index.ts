// import { handleModuleRoutes } from "@/utils/routesMerge";
// import type { AuthRoute } from "@/types/router";
// import type { ImportMeta } from "@/types/env";
//
// const modules = import.meta.glob('./**/*.ts', { eager: true }) as AuthRoute.RouteModule;
//
// export const routeModules = handleModuleRoutes(modules);
